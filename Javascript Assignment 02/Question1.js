let temperature = prompt("Enter the current temperature in Celsius:");
let converter = Number(temperature);
if (temperature > 30) {
    alert("It's a hot day!");
} else {
    alert("The weather is moderate.");
}
